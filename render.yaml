# Render Blueprint - Hyper Backend
# https://render.com/docs/blueprint-spec
# All environment variables are configured in the Render dashboard

services:
  - type: web
    name: hyper-backend
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    # All environment variables (DATABASE_URL, JWT_SECRET, etc.)
    # are already configured in the Render dashboard
